2026-06-06 21:03:00 BST
- Added JSON-driven social post workflow around `instagram.html`.
- Added `posts.example.json` and working `posts.json` format for draft posts.
- Added Buffer helper scripts: `list-buffer-channels.mjs`, `create-buffer-draft.mjs`, `check-posts.mjs`, and `mark-post-status.mjs`.
- Updated README and automation plan with Buffer GraphQL, Cloudinary/public media URL, Instagram metadata, and local status workflow notes.
- Verified script syntax with `node --check`; verified dry-run payloads and readiness checks locally.
- Manual follow-up: keep adding Cloudinary `publicImageUrl` values before sending Instagram drafts to Buffer.

2026-06-06 21:08:14 BST
- Updated `.env.example` with `BUFFER_CHANNEL_SERVICE` and Cloudinary placeholders needed for the planned render/upload automation.
- Manual follow-up: copy the new Cloudinary variables into `.env` with real account values before adding the upload script.

2026-06-06 21:19:09 BST
- Added Playwright/Cloudinary automation: `render-post.mjs`, `upload-cloudinary.mjs`, and `prepare-post.mjs`.
- Added `package.json` with `playwright-core` and generated `package-lock.json` via `npm install`.
- Exposed `window.renderPostForExport` in `instagram.html` for scriptable canvas exports.
- Ran real prepare for `wolds-record-005-dog-profile`; rendered PNG to `generated/`, uploaded it to Cloudinary, and wrote `publicImageUrl`, `cloudinaryPublicId`, `uploadedAt`, and `status: uploaded` to `posts.json`.
- Verified with `npm run check` and `node scripts/check-posts.mjs posts.json`.
- Note: Playwright Chrome launch fails inside the restricted sandbox but works outside it on the local machine.

2026-06-06 21:35:00 BST
- Added editable review support in `instagram.html`: previous/next navigation, post field editing, caption/hashtags/alt text editing, and save/export for loaded JSON.
- Added `logoPath` and `photoPath` support so posts can reference local assets that render into PNG exports.
- Added `assets/logos/` and `assets/photos/` library folders.
- Added `scripts/process-posts.mjs` for batch prepare/send workflows with `--limit`, `--dry-run`, and `--force`.
- Updated README, automation plan, package scripts, `posts.example.json`, and `posts.json`.
- Verified with `npm run check`, batch send dry-run, `node scripts/check-posts.mjs posts.json`, and Playwright render outside the sandbox.

2026-06-06 21:44:46 BST
- Reset `posts.json` and `posts.example.json` to three fresh square Wolds Record draft posts for review.
- New drafts: `wolds-record-006-paper-notes`, `wolds-record-007-vet-reports`, and `wolds-record-008-canine-only`.
- Verified JSON parsing, `npm run check`, and `node scripts/check-posts.mjs posts.json`.
- Current expected status: all three posts are blocked only because `publicImageUrl` is intentionally blank before render/upload.

2026-06-06 21:56:43 BST
- Updated `posts.json` and `posts.example.json` so all three draft posts use `assets/logos/wolds-record-logo-transparent-small.png`.
- Assigned `assets/photos/dog-image.png` to `wolds-record-006-paper-notes`, left `wolds-record-007-vet-reports` without a photo, and assigned `assets/photos/dog-image-2.png` to `wolds-record-008-canine-only`.
- Fixed canvas export for local image paths by embedding local assets as data URLs in `render-post.mjs` and improving browser-side image loading in `instagram.html`.
- Verified JSON parsing, `npm run check`, `node scripts/check-posts.mjs posts.json`, and Playwright render of a logo/photo post outside the sandbox.

2026-06-07 07:22:06 BST
- Expanded `posts.json` to 20 square Wolds Record draft posts, avoiding the three already-live themes supplied by the user.
- Synced `posts.example.json` to the same 20-post draft set for review/testing.
- Rotated available `assets/photos/` images across the set, with a few intentional no-photo posts for visual variety.
- Verified JSON parsing, unique IDs, all-square aspect ratios, `npm run check`, and Playwright render smoke tests for one photo post and one no-photo post outside the sandbox.
- Current expected status: all 20 posts are drafts and blocked only because `publicImageUrl` is blank before review/render/upload.

2026-08-03 Installed the BWH AI workflow (bwh-adopt) for two agent hosts.
- Source: bwh-ai-workflow plugin cache v0.1.0 (no git metadata available, so no commit SHA could be pinned).
- Added `.claude/` and `.agents/`, each with `skills/bwh-*` (7 skills) and `contracts/` (8 contracts) plus a `bwh-ai-workflow.lock`.
- Added host-neutral adapter at `adapters/wolds-record-social-media/` (README.md + context-map.md), built from the workflow `_template`.
- Did NOT reuse the bundled `adapters/wolds-record/` adapter: it describes a Supabase app with typecheck/test/lint and org scoping, none of which exist here.
- Verified `npm run check` and `npm run posts:check` (20 sent, 0 blocked), skill frontmatter on all 7 skills, and that every `../../contracts/*.md` reference resolves in both homes.
- Manual follow-up: no `CLAUDE.md` exists for the Claude Code host, and the adapter's spec/PRD/review/archive paths are left as explicit placeholders.

2026-08-03 Added CLAUDE.md and the plans/ workflow directory tree.
- Created `CLAUDE.md` at the repository root, mirroring `AGENTS.md` rules plus validation commands, guardrails, adapter pointers, and the installed-workflow note. `AGENTS.md` left unchanged; the two must be kept in sync.
- Created `plans/` with `discovery/`, `specs/`, `tasks/`, `reviews/`, `output-testing/`, and `archive/` (each with a `.gitkeep`), plus `plans/README.md` documenting layout, naming, classification, and the on-completion checklist.
- Updated `adapters/wolds-record-social-media/README.md` and `context-map.md` to point every planning, review, output-testing, archive, classification, and manifest rule at the new `plans/` paths.
- Verified `npm run check` and `npm run posts:check` (20 sent, 0 blocked) still pass.
- Manual follow-up: PRD/task record schema, test-framework decision, branch/commit/review/release policy, and the render smoke-check command are still open in the adapter.

2026-08-04 Added Instagram Reel support (spec-driven change: instagram-reel-support).
- New: `video/` HyperFrames composition layer (brand tokens, shared scene CSS/JS, three templates), `scripts/render-video.mjs`, `scripts/prepare-video.mjs`, `scripts/lib/content.mjs`, `test/` (32 checks).
- Modified: `check-posts.mjs`, `create-buffer-draft.mjs`, `upload-cloudinary.mjs`, `process-posts.mjs`, `instagram.html`, `package.json`, `posts.example.json`, `README.md`.
- `format: "image"|"reel"` routes render/upload/Buffer-asset; a record with no `format` still behaves exactly as a static image.
- Verified: `npm run check` PASS; `npm test` 32/32; `posts.json` unmodified (0 ready, 0 blocked, 20 sent); static PNG md5 17f2aa4ff9326356eaf434e67a9d6ba3 identical pre/post change; genuine MP4s rendered at exactly 12.000s and 15.000s, 1080x1920 h264.
- Two integration defects found and fixed: HyperFrames --strict-variables rejects array variables (now JSON-encoded); editor preview pacing diverged from composition weights and omitted product-feature's benefit scene.
- Manual follow-up: Cloudinary video upload and Buffer reel draft are UNEXERCISED (no .env, external writes need authorisation); no product screenshots exist so product-feature has no rendered example; independent review agent failed on a session limit so review was implementer-performed.

2026-08-04 Independent review of Reel support, and resolution of its findings.
- Independent reviewer returned NOT READY (2 blocking, 8 should-fix). Verdict was correct; the earlier implementer-run review had missed both blocking issues.
- B1: compositions left their own placeholder copy in the DOM when a field was empty, so a reel missing captions rendered marketing text the operator never wrote. Fixed in `video/components/scene.js` (clear instead of fall back) and `scripts/lib/content.mjs` (per-scene content validation, required cta, required product-feature benefit).
- B2: the previously recorded "composition lint 0 errors" evidence never ran - `hyperframes lint` scanned zero files and still reported errorCount 0. Added `scripts/lint-compositions.mjs` (`npm run lint:compositions`), which fails loudly on a zero-file scan.
- Also fixed: Cloudinary folder-prefix nesting on re-upload (billed duplicates, stale video served), editor `resolveFormat` divergence, batch aborting on one bad format, dead SCENE_WEIGHTS holding known-wrong pacing, format/instagramType mismatch, unpinned hyperframes, non-JSON upload error handling, silent template coercion. Added Buffer payload integration tests.
- Verified: `npm run check` PASS; `npm test` 52/52; `npm run lint:compositions` 3 ok; posts.json unmodified (0 ready, 0 blocked, 20 sent); static PNG md5 17f2aa4ff9326356eaf434e67a9d6ba3 unchanged; both reels re-rendered at exactly 12.000s and 15.000s.
- Manual follow-up: Cloudinary video upload and Buffer reel drafts still never executed (no .env); product-feature still has no rendered example; `shouldShareToFeed` defaults true for Reels and needs a product decision.

2026-08-04 Resolved the open shouldShareToFeed product decision.
- Human decision: Instagram Reels DO cross-post to the main feed by default (reach over grid curation). Default `true` kept, but made deliberate rather than inherited.
- Added `resolveShareToFeed` + `SHARE_TO_FEED_DEFAULT` to `scripts/lib/content.mjs` as the single source of the rule, so `create-buffer-draft.mjs` and `check-posts.mjs` cannot diverge (the S2 failure mode from review round 2).
- Non-boolean values are now rejected instead of coerced: `"shouldShareToFeed": "false"` is truthy in JS and would have silently inverted the author's intent.
- `check-posts.mjs` now prints `shareToFeed=<value>` on every reel, so a defaulted `true` is visible before publishing, and validates the field on all Instagram posts.
- Kept the field on image posts: Instagram ignores it there, but the pre-Reel payload carries it and byte-identical image payloads are an acceptance criterion. Verified the payload for `wolds-record-006-paper-notes` is identical to `git show HEAD:scripts/create-buffer-draft.mjs` output.
- Documented in README ("Buffer draft flow for Reels"); recorded in `plans/tasks/` and `plans/reviews/instagram-reel-support.md`.
- Verified: `npm run check` PASS; `npm test` 55/55 (3 new); `npm run lint:compositions` 3 ok; `npm run posts:check` 0 ready / 0 blocked / 20 sent; `posts.json` unmodified.
- Manual follow-up unchanged: Cloudinary video upload and Buffer reel drafts still never executed (no .env); product-feature still has no rendered example.

2026-08-04 Wrote the human output-testing script for instagram-reel-support.
- Added `plans/output-testing/instagram-reel-support.md`: 6 phases covering the three parts of the pipeline that have never executed (Cloudinary video upload, Buffer reel drafts, product-feature), plus the shouldShareToFeed decision and the B1 placeholder-copy guard.
- Script uses a scratch `posts.testing.json` copied from `posts.example.json`; `posts.json` is read-only throughout (phase 5) per the live-data guardrail. Every Buffer step is a draft; nothing publishes.
- Phase 0 expected outputs were executed and recorded verbatim, not guessed: `npm test` 55/55, `lint:compositions` 3 ok, `posts:check` 20 sent, and `check-posts posts.testing.json` 0 ready / 22 blocked / 0 sent with `shareToFeed=true` on both reels.
- Phase 4 (B1) was executed by the agent to confirm the documented commands work: rendered `photo-story` from a hand-edited variables file with scene 2's caption blanked, via `npx hyperframes render --strict-variables` (bypassing CLI validation), then extracted the frame at t=8s. The scene rendered with the photo and accent rule and NO text - the B1 fix holds in a real render, not only in tests. Scene 1 at t=6s still showed its caption. Test artifacts deleted.
- Phase 1.4 is the S1 regression check (upload the same reel twice, expect no duplicate asset and no `wolds-record/wolds-record/` nesting) - the defect most likely to still be real, since it was fixed against a never-executed path.
- Manual follow-up: the script is written but NOT executed. Cloudinary and Buffer phases need `.env` credentials and human authorisation; `product-feature` still needs a real product screenshot supplied by the human.

2026-08-04 Executed output testing for instagram-reel-support (human authorised the outward-facing writes).
- Cloudinary PASSED, first ever real execution. Uploaded both reels; `wolds-record/wolds-record-reel-001.mp4` served over HTTP 200 byte-identical to the local file (995,771 bytes, h264 1080x1920 12.000000s).
- S1 (folder nesting causing billed duplicates) CONFIRMED FIXED against the live service: re-uploading the same reel returned an identical URL and public_id, and the Cloudinary Admin API shows exactly 2 wolds-record video assets with zero `wolds-record/wolds-record/` paths.
- Measured consumption (spec open question 3): reel-001 0.95 MB, reel-002 13.52 MB. Photo-story is ~14x heavier than the flat-panel three-point-tip.
- Buffer BLOCKED: HTTP 401 "Access token is not valid" on every call, including read-only `list-buffer-channels.mjs`. BUFFER_API_KEY is 43 chars and token-shaped, so it is expired/revoked/wrong-type rather than a placeholder. Buffer reel drafts and the "also share to feed" toggle remain unproven at the service; the payloads are proven correct by dry-run (shouldShareToFeed true by default, false when opted out, saveToDraft always true).
- Also passed: AC5 (reel without publicVideoUrl rejected, non-zero exit), B1 rendered guard, and Phase 5 live-data regression (posts.json unmodified, PNG md5 17f2aa4ff9326356eaf434e67a9d6ba3).
- Nothing published. No Buffer write ever succeeded; every payload carried saveToDraft: true.
- Results recorded in `plans/output-testing/instagram-reel-support.md`. Change is NOT yet acceptable for archive.
- Manual follow-up: regenerate the Buffer API token and re-run Phase 2 (both reels are already uploaded and staged in posts.testing.json); supply a product screenshot for Phase 3; watch both reels for brand quality. Cloudinary test assets intentionally left in place for the Phase 2 re-run.

2026-08-04 Output testing continued: Phase 3 (product-feature) PASSED; Phase 2 (Buffer) still blocked.
- Buffer token STILL invalid. Retried at 15:37 UTC after the human reported regenerating it: same HTTP 401 "Access token is not valid". `.env` mtime is 07:44 UTC, ~8 hours earlier, and BUFFER_API_KEY is unchanged at 43 chars - the regenerated token was never written to the file. No code defect; nothing to fix in the repo.
- Phase 3 PASSED, first ever render of `product-feature`. Human supplied a dashboard screenshot; cropped 1080x1954 from the 1080x2424 original to remove the phone status bar, browser URL bar, and Android nav bar, saved as `assets/photos/wolds-record-dashboard.png`.
- Added `wolds-record-reel-003` (product-feature) to `posts.example.json`, so all three reel templates now have a worked example. Rendered h264 1080x1920 at exactly 12.000000s.
- Verified all four scenes by frame extraction: title card, product card showing the app screenshot, benefit slide rendering `points[0]` verbatim, and the branded CTA. The benefit slide showed the authored text and NOT the "Wolds Record" kicker - the B1 fallback fix confirmed for the one template it had never been exercised on.
- Staged Phase 2 for a one-command re-run: `posts.testing.json` left in the working tree with both reels `[ready]` (Cloudinary URLs in place, reel-002 set shouldShareToFeed false). Added `posts.testing.json` to `.gitignore` so the scratch file cannot be committed.
- Verified: `npm run check` PASS; `npm test` 55/55; `npm run posts:check` 0 ready / 0 blocked / 20 sent; `posts.json` unmodified.
- Manual follow-up: paste the regenerated Buffer token into `.env`, confirm with `list-buffer-channels.mjs`, then run Phase 2 (draft creation + the "also share to feed" toggle check, ON for reel-001 / OFF for reel-002). Nobody has yet watched a reel end to end for pacing.

2026-08-04 Output testing COMPLETE for instagram-reel-support. Buffer leg passed; all phases green.
- Buffer token now valid. `list-buffer-channels.mjs` returned org "My Organization" and one Instagram channel `woldsrecord`; confirmed BUFFER_CHANNEL_ID matches that channel before any write.
- Created the first two Buffer Reel drafts ever: 6a720c43672517345444aad9 (wolds-record-reel-001) and 6a720c89f57e7ef8ca948d3e (wolds-record-reel-002).
- shouldShareToFeed VERIFIED AT THE SERVICE, not just in our payload: queried both posts back via the Buffer GraphQL read schema (InstagramPostMetadata) - reel-001 status=draft type=reel shouldShareToFeed=true, reel-002 status=draft type=reel shouldShareToFeed=false, both with a VideoAsset. The 2026-08-04 product decision and its opt-out both arrive intact.
- Note: the write schema and read schema differ. Creation sends metadata.instagram.{type,shouldShareToFeed}; reading requires `post(input: PostInput!)` and an inline fragment on `InstagramPostMetadata`. Introspection was needed to find this; recorded here to save the next agent the round trip.
- Nothing published. Org-wide audit of 25 posts: only the 2 created today are new, both status=draft. All others predate this change (12 sent + 8 draft from the 2026-06-07 batch, 3 older sent).
- Observation, not a defect: posts.json marks all 20 records `sent_to_buffer`, but Buffer shows 12 of that batch as sent and 8 still draft. `sent_to_buffer` means "handed to Buffer", not "published" - the two statuses are not meant to track each other.
- Verified: `npm run check` PASS; `npm test` 55/55; `npm run lint:compositions` 3 ok; `npm run posts:check` 0 ready / 0 blocked / 20 sent; `posts.json` unmodified.
- Results table in `plans/output-testing/instagram-reel-support.md` now reads PASSED pending one human check.
- Manual follow-up: (1) watch all three reels end to end for pacing - the only check never performed; (2) decide whether to publish or delete the two Buffer drafts; the Cloudinary assets must stay while the drafts exist since Buffer fetches media from those URLs.

2026-08-04 Output testing ACCEPTED for instagram-reel-support.
- Human viewed all three rendered reels and approved them on look and pacing. No changes requested to composition, timing, or brand treatment. `plans/output-testing/instagram-reel-support.md` verdict set to ACCEPTED.
- Enhancement raised at acceptance and explicitly deferred: the reels are silent and would benefit from background music. `audioPath` is already carried in the schema but unused - the spec recorded that as a deliberate assumption for this iteration. This is a separate future change, NOT part of instagram-reel-support.
- Note for whoever picks up the music work: `hyperframes` has a `beats` command (detect beats in a music track, writes beats/<audio>.json), which suggests beat-synced pacing is feasible. Licensing for any track is an open question and is a human decision.
- Still open, not blocking archive: the two Buffer drafts (6a720c43672517345444aad9 shouldShareToFeed=true, 6a720c89f57e7ef8ca948d3e shouldShareToFeed=false) await a publish-or-delete decision. Their Cloudinary assets must stay while the drafts exist.
- Change is ready for `bwh-archive-change`.

2026-08-04 Archived instagram-reel-support, then committed and pushed.
- Archive bundle: `plans/archive/2026-08-04-instagram-reel-support/` with manifest.md and five artifacts (discovery, specs, tasks, reviews, output-testing). Copies verified md5-identical to originals before the originals were removed; spec and task record set to terminal state ARCHIVED.
- Kept in place: `plans/README.md` (shared, multi-change) and all source-of-truth documents (README.md, CLAUDE.md/AGENTS.md, adapter, posts.example.json, posts.json, this log).
- Branch `feature/instagram-reel-support` pushed; PR #1 opened at https://github.com/simplybenuk/wolds-record-social-media/pull/1. Human chose feature-branch-plus-PR over committing to main, which sets the precedent for the adapter's still-open branch/commit policy placeholder.
- Four grouped commits: f9d4155 workflow scaffold, 7b10ae5 reel pipeline, 9496673 docs, 410450b archive. Note the shouldShareToFeed decision could not be split into its own commit - it touches the same files as the pipeline commit and interactive staging is unavailable - so it is described within 7b10ae5.
- Pre-commit secret scan clean: only .env.example placeholders matched; `.env` and `posts.testing.json` are both git-ignored and confirmed excluded.
- Verified on the committed tree: `npm run check` PASS, `npm test` 55/55, `npm run lint:compositions` 3 ok, `npm run posts:check` 0 ready / 0 blocked / 20 sent, and `git diff 7b8bc9c HEAD -- posts.json` empty (live data untouched by the whole change).
- Manual follow-up: human intends to publish the two Buffer drafts; their Cloudinary assets must remain while referenced. Background music remains deferred to a separate change.

2026-08-04 Independent review marked wolds-record-campaign-review-slice READY FOR HUMAN TESTING.
- Added `plans/reviews/wolds-record-campaign-review-slice.md`; verified typecheck, 94 tests, posts check, production build, dependency provenance, data/security boundaries, and a complete 390x844 mobile journey with zero console errors or overflow.
- Manual follow-up: run one live OpenAI output test, assess brand/content/visual quality, and confirm photo publication rights; this slice must stop at local approval.

2026-08-04 23:17 UTC Developed wolds-record-campaign-review-slice on `feature/wolds-record-campaign-review-slice`.
- Added the Next/SQLite campaign app, fixed brand/generation/domain contracts, reusable static renderer, mobile review actions, migrations, tests, setup docs, and BWH task/output artifacts; legacy `posts.json` remained unchanged.
- Verified `npm run check`, 94 tests, posts validation, production build, three Reel composition checks, clean data/security scans, and the complete 390x844 fixture flow; independent review is READY FOR HUMAN TESTING.
- Manual follow-up: complete the prepared human checklist with one live OpenAI campaign, brand/visual judgment, and photo-rights confirmation; do not publish from this slice.

2026-08-08 22:18 UTC Reconstructed and prepared wolds-record-campaign-review-slice for GitHub publication.
- Restored the reviewed branch from recorded development patches, regenerated the official dependency lock, and tightened clean-install TypeScript boundaries for the published Drizzle package.
- Verified typecheck, production build, 60 legacy tests, posts validation, composition linting, lockfile provenance, diff hygiene, and unchanged `posts.json`; Chrome-based tests could not relaunch in the current restricted sandbox (`socket() Operation not permitted`) but retain the prior 94-test and mobile-review evidence.
- Manual follow-up: run the prepared live OpenAI human-testing checklist using server-only `OPENAI_API_KEY`; do not publish from this slice.

2026-08-08 22:32 UTC Clean-checkout verification of published commit c412072; fixed a test-harness regression.
- Verified from a fresh `git worktree` plus `npm ci`, because prior evidence came from environments that reconstructed the branch from patches, had no npm registry access, or could not launch Chrome. This was the first full run of this exact commit with a working browser.
- Defect found and fixed in `scripts/run-ts-tests.mjs`: the esbuild bundle was written to an OS temp directory while `playwright-core` stayed external, so the bundled tests could not resolve it from `/tmp` and every real render failed as `browser_unavailable`. Two integration tests failed on any clean checkout. The bundle is now emitted under `node_modules/.cache/wolds-studio-tests/` and removed after the run.
- Evidence the fix restored real coverage: TS suite duration went from 0.6s to 12.4s and now writes genuine 1080x1080 PNGs.
- Verified after the fix: `npm run check` (incl. `tsc --noEmit`) PASS; `npm test` 34/34 TS tests plus legacy suites PASS; `npm run posts:check` 0 ready / 0 blocked / 20 sent; `npm run lint:compositions` 3 ok; `npm run build` PASS with the three campaign routes; `drizzle/0000_campaign_review.sql` applies to an empty SQLite database with foreign keys on; `npm run verify:mobile` full 390x844 journey PASS, 0 console errors, no overflow.
- `posts.json` has no diff from `main`. No Buffer, Cloudinary, or Meta call occurred, and no live OpenAI request was made.
- Advisory: `npm audit` reports 3 high-severity advisories, all transitive to Next.js 16.2.10 (next, postcss, sharp) with no fix at that version; registry latest is 16.3.0. Low practical risk while the app is loopback-only, but worth a bump before any wider exposure.
- Manual follow-up: human output testing remains the open gate. No PR was opened for this branch; that decision is the human's.

2026-08-04 21:15 UTC — Added plans/discovery/wolds-record-campaign-review-slice.md recommending a mobile brief-to-approved-campaign pilot for Wolds Record. Verified against VISION.md, current post schema, renderer, workflow adapter, and planning conventions. Manual follow-up: confirm pilot brand, image-only scope, and live-AI acceptance before specification.

2026-08-04 21:29 UTC — Added plans/specs/wolds-record-campaign-review-slice.md at READY FOR HUMAN APPROVAL. Spec covers the mobile campaign/review loop, durable AI attempts, SQLite model, renderer boundary, security, acceptance criteria, and validation. Verified against discovery, VISION.md, current OpenAI guidance, current renderer/tests, and workflow conventions; `git diff --check` required. Manual follow-up: human spec approval or refinement.
(The two entries above were written on 2026-08-04 in the main working tree and were lost when this branch was reconstructed from patches on 2026-08-08; restored here out of chronological order rather than rewriting the log.)

2026-08-09 UTC Bumped Next.js 16.2.10 to 16.3.0 to clear the three high-severity advisories.
- `package.json` and `package-lock.json` only; the pinned-tarball convention is unchanged. `next build` regenerated `next-env.d.ts` with the new `root-params.d.ts` reference. 16.3.0 no longer nests its own `postcss`/`sharp`, which is why the lock shrinks by ~500 lines.
- `npm audit` goes from 3 high (next, postcss, sharp; no fix at 16.2.10) to 0 vulnerabilities, confirmed after a clean `npm ci` from the regenerated lock.
- Verified in a fresh worktree on this exact tree: `npm run check` (incl. `tsc --noEmit`) PASS; `npm test` 34/34 TS tests in 12.5s plus 60/60 legacy tests PASS; `npm run posts:check` 0 ready / 0 blocked / 20 sent; `npm run lint:compositions` 3 ok; `npm run build` PASS with the three campaign routes; `npm run verify:mobile` full 390x844 fixture journey PASS, 0 console errors, no overflow (run against `npm start` on port 3101 because another process holds 3000).
- `posts.json` has no diff from `main`. No Buffer, Cloudinary, or Meta call occurred, and no live OpenAI request was made.
- Also cleaned stale untracked copies of the discovery and spec documents out of the main working tree; both were superseded by the versions committed on this branch.
- Manual follow-up: human output testing remains the open gate. Still no PR opened for this branch; that decision is the human's.

2026-08-09 05:25 UTC — Added plans/discovery/multi-brand-campaign-packs.md, a discovery brief for adding Wolds Canine Massage and Wolds Canine Therapy Academy brand packs to the campaign loop. Human confirmed two directions: per-brand palette and logo are in scope (fonts deferred), and packs are drafted from the live public sites then human-reviewed before any live generation. Key evidence: the static palette is a hardcoded constant at instagram.html:716-720, duplicated at instagram.html:623-627 and video/brand/tokens.css:5-10, so a second brand is not config-only; assets/logos/ holds only the Record logo; posts.json carries `brand` as an unvalidated label. Docs-only change; no source files or posts.json touched.

2026-08-09 05:35 UTC — Added plans/specs/multi-brand-campaign-packs.md at READY FOR HUMAN APPROVAL. Spec covers three brand packs, semantic-role palettes owned by the pack, renderer palette parameterisation with Record values as the absent-palette fallback, per-brand pillar/asset allow-lists generated into the strict output schema, brand-specific prohibited claims (clinical for Massage, accreditation for Academy), WCAG AA contrast as a blocking gate, and a human pack-review gate before any live API call. Acceptance criterion 12 is the real test: adding the third pack must require no change to core concepts.
- Section 0 is a hard development precondition: this spec is written against the wolds-record-campaign-review-slice specification, which is NOT in this repository. Those pilot discovery/spec files existed as uncommitted working files and were removed between sessions; they were never committed, so they are not recoverable from git history. Every R-number reference in this spec is currently uncheckable.
- Also noticed at this point: agents_log.txt had been reverted to its committed state, dropping the two 2026-08-04 entries that recorded the pilot discovery and spec. Those entries are not restored here because their content is not recoverable; this note records the gap.
- Docs-only change. No source files, posts.json, or scripts touched, so no validation commands were applicable.
- Manual follow-up: (1) restore or re-create the pilot specification before this spec can be re-verified or developed; (2) human spec approval; (3) human-supplied Massage/Academy logos, confirmed facts, prohibited claims, and palettes are needed before live generation.

2026-08-09 05:55 UTC — Merged feature/wolds-record-campaign-review-slice into feature/multi-brand-campaign-packs and re-verified the multi-brand spec against the delivered code.
- Merge had one conflict, in agents_log.txt; resolved by keeping both sides in chronological order, no entries dropped. `git diff origin/feature/wolds-record-campaign-review-slice HEAD` shows only agents_log.txt plus the two multi-brand planning documents, so this branch adds no code and the pilot's clean-worktree validation still stands.
- Retracting the previous entry's claim that the pilot artifacts were unrecoverable: they were committed on the pilot branch all along, and that branch's own log (2026-08-09 Next.js entry) records deleting the superseded untracked copies from the main working tree. No content was lost.
- Section 0 re-verification found five wrong assumptions in the drafted spec, now corrected: (1) brands/record/brand.json already has a visualStyle object with palette, headlineFont, bodyFont, imageOpacity, safeMode and aspectRatio, validated by brandPackSchema and consumed by nothing; (2) the palette ships with literal colour names, not semantic roles; (3) brandPackSchema:153-158 currently REQUIRES every pack to allow every global pillar, the inverse of per-brand allow-lists; (4) legacy-post-adapter.ts hardcodes kicker/imageOpacity/safeMode/aspectRatio, and its opacity of 18 contradicts the pack's 17; (5) the brand_id CHECK constraint is in SQL on both tables, so a migration is required.
- Two scope decisions changed on that evidence, both confirmed by the human: fonts move INTO scope (the pack slot exists and is ignored), and the palette keys are renamed to semantic roles.
- Verified on the merged tree: `npm run posts:check` 0 ready / 0 blocked / 20 sent, and `git diff main -- posts.json` empty. `npm run check` could not complete here because this working tree has no node_modules for the newly merged devDependencies (`tsc: not found`); no code changed in the merge, so the pilot branch's own clean-worktree run is the standing evidence.
- Manual follow-up: (1) PR #2 human output testing is still the open gate and may change these structures again; (2) confirm whether the Record opacity shift from 18 to 17 is acceptable, or correct the pack to 18; (3) human approval of this spec; (4) Massage/Academy logos, confirmed facts, prohibited claims, palettes and fonts still needed before live generation.

2026-08-09 (local) — multi-brand campaign packs: brand boundary slice
Files changed: instagram.html (palette/font/handle parameterisation, live swatches);
  src/lib/brand/packs.ts (new: one loader, alias table, renderer brand input);
  src/lib/brand/record.ts (thin facade over the loader);
  src/features/campaigns/types.ts (BRAND_IDS widened, LEGACY_BRAND_ALIASES, pillar union);
  src/features/campaigns/schemas.ts (semantic palette roles; every-pillar assertion removed);
  brands/record/brand.json (palette keys renamed, version 2026-08-09.1);
  src/lib/rendering/legacy-post-adapter.ts + campaign-renderer.ts (pack-derived identity);
  src/db/index.ts (migration runner now walks the drizzle journal);
  src/db/schema.ts + drizzle/0001_widen_brand_id.sql (brand_id CHECK widened, both tables);
  scripts/check-posts.mjs (brand validated as a warning);
  test/brand-packs.test.ts (new), test/rendering-contract.test.ts (fixture updated);
  plans/tasks/multi-brand-campaign-packs.md (new).
Verification: npm install was required first — the pilot's next/react/typescript/
  drizzle-orm/openai were missing and npm run check failed at "tsc: not found" before
  any edit. After install: npm run check pass; npm test 47 pass / 0 fail;
  npm run posts:check 0 ready / 0 blocked / 20 sent (unchanged, posts.json diff empty);
  npm run lint:compositions 3 ok; npm run build pass. Record render md5 regression
  PASS — byte-identical across the problem, feature and hook templates.
Manual follow-up:
  - Human pack review (spec §9.2) before any Massage or Academy pack is drafted or sent
    to the API. Not waived. woldscanine.com claims "Accredited, science-led training"
    with no awarding body named — the Academy pack must prohibit that claim.
  - Human-supplied logo files for Massage and Academy.
  - npm run verify:mobile before the brand-selection UI task lands.
  - Record's application-rendered image opacity moves 18 -> 17 (pack is now
    authoritative). Legacy CLI output is unaffected. Confirm against PR #2 output testing.
  - No external service was contacted. No Buffer, Cloudinary or Meta call was made.

2026-08-09 (local) — multi-brand campaign packs: agent review fixes
Files changed: src/db/index.ts (migration DDL now runs in a transaction with rollback;
  foreign_keys restored in a finally; foreign_key_check read back and enforced);
  drizzle/0001_widen_brand_id.sql (inert pragma check removed, comment corrected);
  src/features/campaigns/schemas.ts (unreachable pillar refinement removed);
  src/lib/brand/packs.ts (Object.hasOwn guards on pack and alias lookups);
  src/lib/rendering/campaign-renderer.ts (resolves the pack from post.brandId per R4;
  asset allow-list spans all enabled packs);
  instagram.html (style resolved before the reel early-return);
  scripts/check-posts.mjs (accurate message for a non-string brand);
  test/brand-packs.test.ts (interrupted-migration, dependent-rows and prototype cases);
  plans/tasks/multi-brand-campaign-packs.md (review record).
Verification: npm run check pass; npm test 50 pass / 0 fail; npm run posts:check
  0 ready / 0 blocked / 20 sent (unchanged, posts.json diff empty);
  npm run lint:compositions 3 ok; npm run build pass; Record render md5 regression
  PASS — still byte-identical.
Manual follow-up: unchanged from the previous entry. No external service was contacted.

2026-08-09 (local) — multi-brand campaign packs: Reel deferral comment (spec R3 second site)
Files changed: video/brand/tokens.css (comment only — records that these tokens are
  deliberately Record-only and are not resolved from the active brand pack, that every
  composition links the file statically so a non-Record post renders Record colours here,
  and points at spec R3 and the matching note in instagram.html).
Reason: spec sec.141 / task 2 required the Reel deferral stated at both sites.
  instagram.html:715 carried it; this file did not.
Verification: npm run lint:compositions 3 ok, 0 failed. No behavioural change — CSS
  comment only; no declaration, selector or token value touched.
Manual follow-up: none. No external service was contacted.

2026-08-09 (local) — multi-brand campaign packs: record the Reel deferral cost
Files changed: plans/tasks/multi-brand-campaign-packs.md (deferred table gains an
  R3 Reel palette row; new "Carried forward to the Reel slice" section records four
  facts no artifact held before — the editor Reel preview renders Record colours for
  any brand because activePalette reaches only draw() and paintSwatches() and not the
  Reel preview DOM; review finding 6 fixed the between-posts state leak, not this;
  tokens.css is linked statically by all three compositions; tokens.css also carries
  motion, spacing, type scale and safe-area insets, so brand-vs-house-style needs
  deciding. Plus scope context that src/ has no reel concept at all).
Verification: documentation only, no code touched, no validation gate applicable.
Manual follow-up: not reachable while wolds-record is the only pack on disk; becomes
 reachable when the T4 packs land. No external service was contacted.

2026-08-09 UTC Documented two setup gaps found while verifying the Next 16.3.0 bump.
- `README.md` and `plans/output-testing/wolds-record-campaign-review-slice.md`: note the `PORT`/`APP_URL` override when port 3000 is already taken, that `verify:mobile` drives an already-running server rather than starting one, and that human testing should install with `npm ci`.
- Documentation only; no code, dependency, or `posts.json` change, so no revalidation was required.

2026-08-10 19:32 UTC — enable LAN dev origin and switch mobile test server to production
Files changed: next.config.ts (allow 192.168.1.188 in Next.js dev origins), agents_log.txt.
Verification: npm run build passed; production server on 0.0.0.0:3011 returned HTTP 200 over LAN and browser verification loaded the campaign page.
Manual follow-up: reload the mobile campaign and retry the previously pending generation.

2026-08-10 UTC — implement approved Massage and Academy campaign packs
Files changed: brands/massage/brand.json and prompt.md, brands/academy/brand.json and
prompt.md, two approved logo assets, pack-scoped schemas/generation/actions/repository,
campaign creation and review UI, mobile verifier, tests, README.md, automation-plan.md,
multi-brand task/spec/output-testing artifacts, and layout metadata.
Verification: npm run check, npm test (60 legacy + 53 application tests), npm run
posts:check (0 ready / 0 blocked / 20 sent), npm run lint:compositions, npm run build,
agent-browser smoke, and Record/Massage/Academy 390x844 mobile journeys all passed;
posts.json unchanged. No live OpenAI or publication service was contacted.
Manual follow-up: independent agent review, then bounded human live generation and
visual/content testing for Massage and Academy.

2026-08-10 UTC — independent review closed the legacy brand-resolution gap
Files changed: scripts/lib/legacy-brand.mjs, scripts/render-post.mjs,
test/legacy-brand.test.mjs, package.json, plans/reviews/multi-brand-campaign-packs.md,
plans/specs/multi-brand-campaign-packs.md, plans/tasks/multi-brand-campaign-packs.md.
Verification: npm run check, npm test (63 legacy + 53 application tests), build,
posts check, Reel lint, and three known Record CLI md5 renders passed; posts.json
unchanged. Mobile and agent-browser evidence remained green from the preceding gate.
Manual follow-up: bounded human live generation and visual/content testing for Massage
and Academy; successful acceptance can proceed to archive.

2026-08-10 20:46 UTC — redeployed the reviewed branch to the local production server
Files changed: none; rebuilt the current worktree and restarted Next.js on
0.0.0.0:3011.
Verification: npm run build passed; /campaigns/new returned HTTP 200; browser smoke
found meaningful content, all three brand options, no error overlay, and no blank page.
Manual follow-up: keep the local production process running for human output testing.

2026-08-10 20:47 UTC — make LAN mobile access repeatable
Files changed: package.json (added start:lan), README.md (LAN access instructions).
Verification: Next remained bound to 0.0.0.0:3011; the LAN URL at
192.168.1.188:3011/campaigns/new returned HTTP 200; npm run check passed.
Manual follow-up: phone must be on the same trusted Wi-Fi; use the current LAN IP
if DHCP changes it, and do not expose port 3011 publicly.

2026-08-12 UTC — resolved PR #4's merge conflicts against main.
Files changed: README.md and agents_log.txt; retained the branch's LAN access guidance
and complete chronological multi-brand history, avoiding the duplicate main log entry.
Verification: git diff --check and the repository validation suite pending.
Manual follow-up: commit and push the resolved merge when authorized.

2026-08-12 14:54 UTC — specified the Instagram engagement creative system
Files changed: plans/discovery/instagram-engagement-creative-system.md and
plans/specs/instagram-engagement-creative-system.md.
Verification: both artifacts were read back; required discovery/spec sections and
READY FOR HUMAN APPROVAL status are present; git diff --check passed.
Manual follow-up: human review of format intents, carousel range and slice priority;
no development, live generation or external service action was performed.

2026-08-12 UTC — Instagram engagement creative system approved for development
Files changed: plans/specs/instagram-engagement-creative-system.md and agents_log.txt.
Verification: recorded the project owner's explicit approval and advanced the spec
from READY FOR HUMAN APPROVAL to APPROVED FOR DEVELOPMENT.
Manual follow-up: create the dedicated development worktree/branch; no commit,
publication or external service action was performed.

2026-08-12 UTC — prepared the Instagram engagement development worktree
Files changed: no product files; approved discovery/spec artifacts and the append-only
log were transferred to this worktree on feature/instagram-engagement-creative-system.
Verification: source/destination hashes matched, spec status is APPROVED FOR DEVELOPMENT,
git diff --check passed, and the original main worktree was restored clean.
Manual follow-up: begin development in this worktree; changes remain uncommitted.

2026-08-12 15:50 UTC — implemented Instagram portrait and carousel creative system
Files changed: campaign migration/domain/generation/rendering/UI/tests, all three brand
packs/prompts, README.md, automation-plan.md, and the change task artifact.
Verification: check; 63 legacy + 52 TypeScript tests; posts check (0/0/20); Reel lint
(3 ok); build; 390x844 carousel/edit/reload/review browser flow; six-template visual
inspection; legacy PNG md5 unchanged; posts.json byte diff empty.
Manual follow-up: independent agent review, then bounded human fixture/live output testing;
no live generation, upload, publication, commit, or push was performed here.

2026-08-12 15:55 UTC — independent review returned Instagram creative system to development
Files changed: plans/reviews/instagram-engagement-creative-system.md and agents_log.txt.
Verification: npm run check, npm test (63 legacy + 52 TypeScript), posts check (0/0/20),
Reel lint (3 ok), build, git diff --check, and posts.json hash/diff checks passed.
Manual follow-up: resolve the five blocking integrity/approval/render/contract findings and
two should-fix traceability/atomicity findings, rerun all gates, then repeat agent review.

2026-08-12 16:11 UTC — remediated Instagram creative-system agent review findings
Files changed: campaign migration/schema/repository/domain/UI/render validation and focused
tests, README.md, automation-plan.md, task/review traceability, and this append-only log.
Verification: check; 63 legacy + 58 TypeScript tests; posts check (0/0/20); Reel lint
(3 ok); build; successful 390x844 mobile browser flow; legacy PNG md5 and posts.json hash
unchanged; git diff --check passed.
Manual follow-up: repeat independent agent review; no live generation, upload, publication,
commit, push, or pull request was performed here.

2026-08-12 16:34 UTC — remediated migrated-square approval and PNG integrity review findings
Files changed: shared approval readiness, PNG structural validation, migration/PNG tests,
README.md, automation-plan.md, task/review traceability, and this append-only log.
Verification: check; 63 legacy + 58 TypeScript tests; posts check (0/0/20); Reel lint
(3 ok); build; 390x844 mobile browser flow; historical square re-approval preserved bytes;
legacy PNG md5 and posts.json hash unchanged; git diff --check passed.
Manual follow-up: repeat independent agent review; no live generation, upload, publication,
commit, push, or pull request was performed here.

2026-08-12 16:21 UTC — second independent review found a migrated-square approval regression
Files changed: plans/reviews/instagram-engagement-creative-system.md and agents_log.txt.
Verification: independently passed check, 63 legacy + 58 TypeScript tests, posts check,
Reel lint, build, git diff check, posts.json hash/diff, and the isolated 390x844 mobile flow.
Manual follow-up: preserve square-file re-approval without rerender, strengthen complete PNG
validation, rerun all gates, then repeat agent review; human testing remains blocked.

2026-08-12 16:35 UTC — third independent review cleared Instagram creative system for human testing
Files changed: plans/reviews/instagram-engagement-creative-system.md and agents_log.txt.
Verification: independently passed check, 63 legacy + 58 TypeScript tests, posts check,
Reel lint, build, diff/hash checks, isolated 390x844 mobile flow, and extra PNG integrity probes.
Manual follow-up: human-test representative brand/template output and migrated square review;
success hands off to archive, while any defect returns to development.

2026-08-13 05:17 UTC — remediated audience targeting and portrait creative quality
Files changed: brand packs/prompts, fixture copy, six-family portrait renderer, focused tests,
README.md, automation plan, task/review traceability, and this append-only log.
Verification: check; 63 legacy + 60 TypeScript tests; posts check (0/0/20); Reel lint
(3 ok); build; six 1080x1350 visual inspections; isolated Massage 390x844 mobile flow;
posts.json hash unchanged; git diff --check passed.
Manual follow-up: repeat independent agent review before human testing; no live generation,
upload, publication, commit, push, or pull-request action was performed here.

2026-08-13 05:45 UTC — fourth independent review returned R3 to development
Files changed: plans/reviews/instagram-engagement-creative-system.md and this append-only log.
Verification: independently passed check, 63 legacy + 60 TypeScript tests, posts check,
Reel lint, build, legacy/hash checks, and isolated Massage 390x844 mobile flow; inspected
normal and boundary-length renders for all six templates.
Manual follow-up: fix silent portrait-copy truncation and action-panel overlap, add boundary
regressions, rerun all gates, then repeat agent review; human testing remains paused.
2026-08-13 06:00 UTC — Resolved IECS review B7 in `instagram.html`, campaign copy schema/UI, focused tests, and the development task: removed silent portrait clipping, added shared readable copy limits, explicit collision-free regions, exact-boundary six-template renders, and moved decorative elements clear of copy. Verification passed: `npm run check`; `npm test` (63 legacy, 61 TypeScript); posts check; Reel lint; production build; `git diff --check`; and isolated Massage mobile flow at 390×844 with no overflow/console errors. `posts.json` unchanged. Manual follow-up: fresh independent agent review before human testing.
2026-08-13 06:05 UTC — fifth independent review cleared IECS-R4 for human testing.
Files changed: plans/reviews/instagram-engagement-creative-system.md and this append-only log.
Verification: independently passed check, 63 legacy + 61 TypeScript tests, posts check,
Reel lint, production build, legacy/hash checks, exact-boundary visual inspection of all six
templates, and isolated Massage 390x844 mobile flow. Manual follow-up: Ben or Olivia should
test owner/practitioner tone, creative quality, CTA completeness, and phone readability;
acceptance hands off to archive, while any output defect returns to development.
2026-08-13 06:07 UTC — Deployed the independently cleared production build for LAN human testing at `http://192.168.1.188:3000/campaigns/new`; direct host check returned HTTP 200. No live generation, upload, scheduling, publication, commit, push, or PR action was performed. Manual follow-up: keep the host session running during Ben/Olivia testing.
